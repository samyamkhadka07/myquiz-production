'use client';import {useState} from 'react';import {api} from '@/lib/client-api';
export function LearningActivities(){const [result,setResult]=useState(''),[busy,setBusy]=useState(false);return <form className="card form" onSubmit={async e=>{e.preventDefault();setBusy(true);try{const f=new FormData(e.currentTarget);const r=await api<{text:string;ai:boolean}>('learning','POST',{activity:f.get('activity'),text:f.get('text')});setResult(r.text);}catch(x){setResult((x as Error).message);}finally{setBusy(false);}}}><label>Activity<select name="activity"><option value="RAPID_RECALL">Rapid recall</option><option value="MISTAKE_CORRECTION">Mistake correction</option><option value="MATCHING">Matching</option><option value="CLASSIFICATION">Classification</option><option value="EXPLAIN_DIFFERENTLY">Explain differently</option></select></label><label>Verified content or review note<textarea name="text" required maxLength={12000}/></label><button className="button" disabled={busy}>{busy?'Preparing…':'Start activity'}</button>{result&&<section className="card question-text" aria-live="polite">{result}</section>}</form>}
