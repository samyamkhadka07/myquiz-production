'use client';import {useState} from 'react';import {api} from '@/lib/client-api';
export function ReadingMaterialActions({contributionId,status}:{contributionId:string;status:string}){const [message,setMessage]=useState('');async function act(publish:boolean){try{await api(`reading-materials/${contributionId}`,'POST',{publish});setMessage(publish?'Published to the Student Reading Room.':'Archived from the Student Reading Room.');location.reload();}catch(error){setMessage((error as Error).message);}}return <div><div className="toolbar"><button className="button" disabled={status==='PUBLISHED'} onClick={()=>void act(true)}>Publish</button><button className="button secondary" disabled={status==='ARCHIVED'} onClick={()=>void act(false)}>Archive</button></div><small role="status">{message}</small></div>}
