'use client';import {useState} from 'react';import {api} from '@/lib/client-api';
export function ActionButtons({resource,id,actions}:{resource:string;id:string;actions:string[]}){const [message,setMessage]=useState(''),[busy,setBusy]=useState(false);return <div><div className="toolbar">{actions.map(action=><button className="button secondary" disabled={busy} key={action} onClick={async()=>{setBusy(true);try{const path=resource==='processing'&&['run','retry'].includes(action)?`${resource}/${id}/${action}`:resource==='comments'?`${resource}/${id}/moderate`:`${resource}/${id}`;await api(path,'POST',{action});setMessage(`${action.replaceAll('_',' ').toLowerCase()} saved. Refreshing…`);location.reload();}catch(e){setMessage((e as Error).message);}finally{setBusy(false)}}}>{action.replaceAll('_',' ')}</button>)}</div><small role="status">{message}</small></div>}
export function StagedEditor({id,initial}:{id:string;initial:Record<string,unknown>}){const [text,setText]=useState(JSON.stringify(initial,null,2)),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function act(action:'IMPORT'|'IMPORT_VERIFY'|'IMPORT_PUBLISH'|'REJECT'|'NEEDS_REVISION'){setBusy(true);try{const questionId=await api<string|null>(`staged/${id}`,'POST',{action,data:action==='REJECT'?null:JSON.parse(text)});setMessage(questionId?`Saved as canonical question ${questionId}.`:'Review decision saved.');location.reload();}catch(e){setMessage((e as Error).message);}finally{setBusy(false)}}return <div className="form"><label>Canonical question JSON<textarea rows={14} value={text} onChange={e=>setText(e.target.value)}/></label><p className="muted">Import keeps the question in staging. Verify checks the complete answer, four option explanations, taxonomy and provenance. Publish makes the same verified canonical question eligible for student tests.</p><div className="toolbar"><button className="button secondary" disabled={busy} onClick={()=>void act('IMPORT')}>Import only</button><button className="button" disabled={busy} onClick={()=>void act('IMPORT_VERIFY')}>Import and verify</button><button className="button" disabled={busy} onClick={()=>void act('IMPORT_PUBLISH')}>Import, verify and publish</button><button className="button secondary" disabled={busy} onClick={()=>void act('NEEDS_REVISION')}>Needs revision</button><button className="button secondary" disabled={busy} onClick={()=>void act('REJECT')}>Reject</button></div><small role="status">{message}</small></div>}
export function UserAccess({id,role}:{id:string;role:string}){const [value,setValue]=useState(role),[message,setMessage]=useState('');return <div><select aria-label="Role" value={value} onChange={e=>setValue(e.target.value)}><option>STUDENT</option><option>MODERATOR</option><option>ADMIN</option></select><button className="button secondary" onClick={async()=>{try{await api(`users/${id}`,'PATCH',{role:value,tier:'FREE',ends_at:null});setMessage('Access updated.');}catch(e){setMessage((e as Error).message);}}}>Save role</button><small role="status">{message}</small></div>}
export function AdminRequestActions({id}:{id:string}){const [note,setNote]=useState(''),[message,setMessage]=useState('');async function decide(decision:'APPROVE'|'REJECT'){try{await api(`admin-requests/${id}`,'PATCH',{decision,note:note.trim()||null});setMessage(`${decision==='APPROVE'?'Admin access approved':'Request rejected'}. Refreshing…`);location.reload();}catch(e){setMessage((e as Error).message);}}return <div className="form"><label>Decision note (optional)<input value={note} maxLength={1000} onChange={e=>setNote(e.target.value)}/></label><div className="toolbar"><button className="button" onClick={()=>void decide('APPROVE')}>Approve Admin</button><button className="button secondary" onClick={()=>void decide('REJECT')}>Reject</button></div><small role="status">{message}</small></div>}
