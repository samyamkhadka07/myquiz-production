import Link from 'next/link';import {requirePage} from '@/lib/server/auth';
export default async function Page(){await requirePage(true);return <><h1>Canonical CSV</h1><p>Exports use the canonical question contract with spreadsheet-formula protection. Imports must be uploaded as a CSV contribution, then pass row validation, duplicate detection, staging and human review.</p><Link className="button" href="/api/csv/export">Download question bank CSV</Link><p className="card section">Upload CSV files from the Contributions page. Rows with missing answer keys remain in review and cannot enter tests.</p></>;}
