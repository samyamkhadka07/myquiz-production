import {requirePage} from '@/lib/server/auth';import {check} from '@/lib/server/data';
export default async function Page(){const {db}=await requirePage(true);const rows=check(await db.from('profiles').select('id,display_name,role,created_at,entitlements(tier,ends_at)').order('created_at',{ascending:false}).limit(100)) as Array<Record<string,unknown>>;return <><h1>Users and access</h1><div className="table-wrap card"><table><thead><tr><th>Name</th><th>Role</th><th>Entitlement</th><th>Created</th></tr></thead><tbody>{rows.map(r=><tr key={String(r.id)}><td>{String(r.display_name)}</td><td>{String(r.role)}</td><td><pre>{JSON.stringify(r.entitlements)}</pre></td><td>{new Date(String(r.created_at)).toLocaleDateString()}</td></tr>)}</tbody></table></div></>;}
