import {requirePage} from '@/lib/server/auth';import {check} from '@/lib/server/data';
export default async function Page(){const {db}=await requirePage(true);const rows=check(await db.from('comment_reports').select('*,comments(body,status,user_id)').order('created_at',{ascending:false}).limit(100)) as Array<Record<string,unknown>>;return <><h1>Moderation reports</h1>{rows.map(r=><article className="card section" key={String(r.id)}><p>{String(r.reason)}</p><pre>{JSON.stringify(r.comments,null,2)}</pre></article>)}{rows.length===0&&<p className="card">No reports await moderation.</p>}</>;}
