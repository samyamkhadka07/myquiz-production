import {requirePage} from '@/lib/server/auth';import {check} from '@/lib/server/data';
export default async function Page(){const {db}=await requirePage(true);const rows=check(await db.from('processing_jobs').select('id,status,kind,attempt_count,max_attempts,available_at,last_error,created_at,contributions(original_filename)').order('created_at',{ascending:false}).limit(100)) as Array<Record<string,unknown>>;return <><h1>Processing monitor</h1><div className="table-wrap card"><table><thead><tr><th>Job</th><th>Kind</th><th>Status</th><th>Attempts</th><th>Error</th></tr></thead><tbody>{rows.map(r=><tr key={String(r.id)}><td>{String(r.id)}</td><td>{String(r.kind)}</td><td>{String(r.status)}</td><td>{String(r.attempt_count)}/{String(r.max_attempts)}</td><td><pre>{r.last_error?JSON.stringify(r.last_error):'—'}</pre></td></tr>)}</tbody></table></div></>;}
