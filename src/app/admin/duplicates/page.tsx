import {requirePage} from '@/lib/server/auth';import {check} from '@/lib/server/data';import {StagedEditor} from '@/components/admin-workflow-actions';
export default async function Page(){const {db}=await requirePage(true);const rows=check(await db.from('staged_items').select('*,questions!staged_items_duplicate_question_id_fkey(question_text,option_a,option_b,option_c,option_d,source_document,provenance)').eq('status','DUPLICATE').order('created_at',{ascending:false}).limit(100)) as Array<Record<string,unknown>>;return <><p className="eyebrow">Ingestion · canonical identity</p><h1>Duplicate review</h1><p>Compare the extracted candidate with its fingerprint match. Keep one canonical question while preserving valid source provenance.</p>{rows.map(r=><article className="card section" key={String(r.id)}><div className="grid"><section><h2>Candidate</h2><pre>{JSON.stringify(r.question_data,null,2)}</pre></section><section><h2>Canonical match</h2><pre>{JSON.stringify(r.questions,null,2)}</pre></section></div><StagedEditor id={String(r.id)} initial={r.question_data as Record<string,unknown>}/></article>)}{!rows.length&&<section className="card empty-state"><h2>No duplicate candidates</h2><p>Fingerprint and extraction duplicate warnings will appear here.</p></section>}</>}
