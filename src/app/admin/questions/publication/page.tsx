import {requirePage} from '@/lib/server/auth';import {check,taxonomy} from '@/lib/server/data';import {QuestionEditor} from '@/components/question-editor';
export default async function Page(){const {db}=await requirePage(true);const [t,rows]=await Promise.all([taxonomy(),db.from('questions').select('*').eq('lifecycle','VERIFIED').eq('verification_status','VERIFIED').eq('publication_status','DRAFT').order('verified_at',{ascending:true}).limit(50)]);return <><p className="eyebrow">Academic content · release control</p><h1>Question publication queue</h1><p>Only verified draft questions appear here. Publication changes the same canonical question record used by eligible student tests.</p><QuestionEditor taxonomy={t} initial={check(rows)}/></>}
