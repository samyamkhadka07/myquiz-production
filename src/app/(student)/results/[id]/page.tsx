import { redirect } from 'next/navigation';
import { requirePage } from '@/lib/server/auth';
import { check } from '@/lib/server/data';
import { uuidSchema,type AttemptDetail } from '@/lib/contracts';
import { Review } from '@/components/review';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;uuidSchema.parse(id);const {db,profile}=await requirePage();const d=check(await db.rpc('get_attempt',{p_attempt:id})) as AttemptDetail;if(d.attempt.status==='ACTIVE')redirect(`/quiz/${id}`);const [saved,...capabilities]=await Promise.all([db.from('bookmarks').select('question_id').eq('user_id',profile.id),...['ai_tutor','ai_nepali','ai_mnemonics','ai_followups'].map(p=>db.rpc('has_entitlement',{p_feature:p}))]);const allowed=capabilities.map(check) as boolean[];return <><p className="eyebrow">Saved test result</p><h1>{d.attempt.score} / {d.attempt.max_score}</h1><p>{d.attempt.correct_count} correct · {d.attempt.incorrect_count} incorrect · {d.attempt.unanswered_count} unanswered</p><p className="muted">{d.attempt.status==='EXPIRED'?'Time expired. ':''}This practice score is not an MEC percentile or qualification decision.</p><Review detail={d} saved={(check(saved) as {question_id:string}[]).map(s=>s.question_id)} aiCapabilities={{tutor:allowed[0]??false,nepali:allowed[1]??false,mnemonics:allowed[2]??false,followups:allowed[3]??false}}/></>;}
